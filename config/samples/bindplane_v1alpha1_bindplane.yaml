apiVersion: k8s.bindplane.com/v1alpha1
kind: Bindplane
metadata:
  labels:
    app.kubernetes.io/name: bindplane-operator
    app.kubernetes.io/managed-by: kustomize
  name: bindplane-sample
spec:
  config:
    license: "your-license-key-here"
    auth:
      type: system
      username: admin
      password: password
    network:
      host: 0.0.0.0
      port: "3001"
      remoteURL: http://bindplane:3001
    store:
      type: postgres
      postgres:
        host: postgres.example.com
        port: "5432"
        database: bindplane
        username: bindplane
        password: bindplane
  bindplane:
    # podTemplate: # Optional - pod template for bindplane node containers
    # replicas: 3  # Optional - number of replicas for bindplane node (default: 3)
  transformAgent:
    # podTemplate: # Optional - arbitrary pod template fields (affinity, tolerations, nodeSelector, etc.)
    #   metadata:
    #     labels:
    #       custom-label: "value"
    #     annotations:
    #       custom-annotation: "value"
    #   spec:
    #     affinity:
    #       podAntiAffinity:
    #         preferredDuringSchedulingIgnoredDuringExecution:
    #           - weight: 100
    #             podAffinityTerm:
    #               labelSelector:
    #                 matchExpressions:
    #                   - key: app.kubernetes.io/component
    #                     operator: In
    #                     values:
    #                       - transform-agent
    #               topologyKey: kubernetes.io/hostname
    #     tolerations:
    #       - key: "key1"
    #         operator: "Equal"
    #         value: "value1"
    #         effect: "NoSchedule"
    #     nodeSelector:
    #       disktype: ssd
    #     priorityClassName: high-priority
  tsdb:
    # podTemplate: # Optional - arbitrary pod template fields (affinity, tolerations, nodeSelector, etc.)
    #   metadata:
    #     labels:
    #       custom-label: "value"
    #     annotations:
    #       custom-annotation: "value"
    #   spec:
    #     affinity:
    #       podAntiAffinity:
    #         requiredDuringSchedulingIgnoredDuringExecution:
    #           - labelSelector:
    #               matchExpressions:
    #                 - key: app.kubernetes.io/component
    #                   operator: In
    #                   values:
    #                     - tsdb
    #             topologyKey: kubernetes.io/hostname
    #     tolerations:
    #       - key: "key1"
    #         operator: "Equal"
    #         value: "value1"
    #         effect: "NoSchedule"
    #     nodeSelector:
    #       disktype: ssd
    #     priorityClassName: high-priority
    #     initContainers:
    #       - name: init-container
    #         image: busybox:latest
    #         command: ["sh", "-c", "echo init"]
    # storage: # Optional - persistent volume configuration
    #   volumeClaimTemplate:
    #     metadata:
    #       labels:
    #         custom-pvc-label: "value"
    #       annotations:
    #         custom-pvc-annotation: "value"
    #     spec:
    #       accessModes:
    #         - ReadWriteOnce
    #       storageClassName: fast-ssd  # Optional - uses default storage class if not specified
    #       resources:
    #         requests:
    #           storage: 120Gi  # Default is 60Gi if not specified
