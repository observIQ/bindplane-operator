---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: bindplanes.bindplane.com
spec:
  group: bindplane.com
  names:
    kind: Bindplane
    listKind: BindplaneList
    plural: bindplanes
    singular: bindplane
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Bindplane is the Schema for the bindplanes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BindplaneSpec defines the desired state of Bindplane.
            properties:
              auth:
                description: Auth configuration for Bindplane
                properties:
                  password:
                    description: Password for authentication
                    type: string
                  type:
                    description: Type specifies the authentication type (e.g., "system")
                    type: string
                  username:
                    description: Username for authentication
                    type: string
                type: object
              eventBus:
                description: EventBus configuration for Bindplane
                properties:
                  type:
                    description: 'Type specifies the event bus type. Supported values:
                      "local", "nats", "googlePubSub", "azure"'
                    enum:
                    - local
                    - nats
                    - googlePubSub
                    - azure
                    type: string
                required:
                - type
                type: object
              license:
                description: License is the Bindplane license key
                type: string
              mode:
                description: Mode specifies the operational mode(s) for Bindplane
                items:
                  type: string
                type: array
              network:
                description: Network configuration for Bindplane
                properties:
                  host:
                    description: Host specifies the bind address
                    type: string
                  port:
                    description: Port specifies the port to listen on
                    type: string
                  remoteURL:
                    description: RemoteURL specifies the remote URL
                    type: string
                type: object
              store:
                description: Store configuration for Bindplane
                properties:
                  postgres:
                    description: Postgres configuration (only used when type is "postgres")
                    properties:
                      connectTimeout:
                        description: ConnectTimeout specifies the connection timeout
                        type: string
                      database:
                        description: Database specifies the database name
                        type: string
                      host:
                        description: Host specifies the PostgreSQL host
                        type: string
                      maxConnections:
                        description: MaxConnections specifies the maximum number of
                          connections
                        type: integer
                      maxLifetime:
                        description: MaxLifetime specifies the maximum connection
                          lifetime
                        type: string
                      password:
                        description: Password specifies the PostgreSQL password
                        type: string
                      port:
                        description: Port specifies the PostgreSQL port
                        type: string
                      schema:
                        description: Schema specifies the database schema
                        type: string
                      sslmode:
                        description: SSLMode specifies the SSL mode
                        type: string
                      statementTimeout:
                        description: StatementTimeout specifies the statement timeout
                        type: string
                      username:
                        description: Username specifies the PostgreSQL username
                        type: string
                    required:
                    - host
                    type: object
                  type:
                    description: Type specifies the store type. Currently only "postgres"
                      is supported.
                    enum:
                    - postgres
                    type: string
                required:
                - postgres
                - type
                type: object
            required:
            - license
            - store
            type: object
          status:
            description: BindplaneStatus defines the observed state of Bindplane.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
