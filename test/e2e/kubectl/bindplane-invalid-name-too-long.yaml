apiVersion: k8s.bindplane.com/v1alpha1
kind: Bindplane
metadata:
  name: bindplane-name-that-is-definitely-longer-than-fortyseven
spec:
  config:
    license: "test-license"
    store:
      postgres:
        host: postgres.postgres.svc
