apiVersion: k8s.bindplane.com/v1alpha1
kind: Bindplane
metadata:
  name: bindplane-invalid-argo-rollout-strategy
spec:
  config:
    license: "test-license"
    store:
      postgres:
        host: postgres.postgres.svc
  bindplane:
    argoRollout:
      enabled: true
    strategy:
      type: RollingUpdate
