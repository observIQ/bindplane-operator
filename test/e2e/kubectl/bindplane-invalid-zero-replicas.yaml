apiVersion: k8s.bindplane.com/v1alpha1
kind: Bindplane
metadata:
  name: bindplane-invalid-zero-replicas
spec:
  config:
    license: "test-license"
    store:
      postgres:
        host: postgres.postgres.svc
  bindplane:
    replicas: 0
