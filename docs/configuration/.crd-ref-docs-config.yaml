# Configuration for crd-ref-docs (https://github.com/elastic/crd-ref-docs)
# Used by: make generate-api-docs
processor:
  ignoreTypes:
    - "BindplaneList$"
  ignoreFields:
    - "status$"
    - "TypeMeta$"

render:
  kubernetesVersion: "1.31"
